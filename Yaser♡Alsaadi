-- LocalScript
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local PlayerGui = player:WaitForChild("PlayerGui")

-- رابط Discord (نسخ)
local discordLink = "https://discord.gg/czXJPUQMj"

-- إنشاء ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CosmicGUI"
screenGui.Parent = PlayerGui

-- نافذة GUI (Draggable + تأثير فضائي)
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 500, 0, 350)
frame.Position = UDim2.new(0.5, -250, 0.5, -175)
frame.BackgroundColor3 = Color3.fromRGB(5, 5, 20)
frame.BorderSizePixel = 0
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.Parent = screenGui
frame.Active = true
frame.Draggable = true

-- تدرج فضائي
local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 0, 30)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(40, 0, 70))
}
gradient.Rotation = 45
gradient.Parent = frame

-- عنوان GUI
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0,50)
title.Position = UDim2.new(0,0,0,0)
title.Text = "المطورعلاوي وا المطور اورانس"
title.TextColor3 = Color3.fromRGB(173, 216, 230)
title.Font = Enum.Font.GothamBold
title.TextSize = 28
title.BackgroundTransparency = 1
title.Parent = frame

-- Server Name
local serverName = Instance.new("TextLabel")
serverName.Size = UDim2.new(0, 300, 0, 30)
serverName.Position = UDim2.new(0.5, -150, 0, 60)
serverName.Text = "Server: B23K (BYK)"
serverName.TextColor3 = Color3.fromRGB(135, 206, 250)
serverName.Font = Enum.Font.GothamBold
serverName.TextSize = 22
serverName.BackgroundTransparency = 1
serverName.Parent = frame

-- زر تشغيل الأنيميشن
local playButton = Instance.new("TextButton")
playButton.Size = UDim2.new(0, 220, 0, 50)
playButton.Position = UDim2.new(0.5, -110, 0, 110)
playButton.Text = "✨ تشغيل الأنيميشن"
playButton.TextColor3 = Color3.fromRGB(255, 255, 255)
playButton.Font = Enum.Font.GothamBold
playButton.TextSize = 20
playButton.BackgroundColor3 = Color3.fromRGB(70, 0, 100)
playButton.BorderSizePixel = 0
playButton.Parent = frame

local playGradient = Instance.new("UIGradient")
playGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(100,0,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0,255,255))
}
playGradient.Rotation = 45
playGradient.Parent = playButton

playButton.MouseButton1Click:Connect(function()
    local character = player.Character
    if character and character:FindFirstChild("Humanoid") then
        local hum = character.Humanoid
        local anim = Instance.new("Animation")
        anim.AnimationId = "rbxassetid://101852027997337"
        local track = hum:LoadAnimation(anim)
        track:Play()

        game.StarterGui:SetCore("SendNotification",{
            Title = "تم التشغيل",
            Text = "الأنيميشن بدأ ✨",
            Duration = 4
        })
    end
end)

-- زر نسخ رابط Discord
local discordButton = Instance.new("TextButton")
discordButton.Size = UDim2.new(0, 220, 0, 50)
discordButton.Position = UDim2.new(0.5, -110, 0, 180)
discordButton.Text = "🌠 نسخ رابط Discord"
discordButton.TextColor3 = Color3.fromRGB(255, 255, 255)
discordButton.Font = Enum.Font.GothamBold
discordButton.TextSize = 20
discordButton.BackgroundColor3 = Color3.fromRGB(50, 0, 100)
discordButton.BorderSizePixel = 0
discordButton.Parent = frame

local discordGradient = Instance.new("UIGradient")
discordGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0,255,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255,0,255))
}
discordGradient.Rotation = 90
discordGradient.Parent = discordButton

discordButton.MouseButton1Click:Connect(function()
    pcall(function()
        setclipboard(discordLink)
    end)
    game.StarterGui:SetCore("SendNotification", {
        Title = "تم النسخ",
        Text = "رابط الديسكورد تم نسخه 🌌",
        Duration = 4
    })
end)

-- زر إغلاق GUI
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 35, 0, 35)
closeButton.Position = UDim2.new(1, -45, 0, 10)
closeButton.Text = "✖"
closeButton.TextColor3 = Color3.fromRGB(255, 100, 100)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 20
closeButton.BackgroundTransparency = 0.5
closeButton.BackgroundColor3 = Color3.fromRGB(20, 0, 40)
closeButton.BorderSizePixel = 0
closeButton.Parent = frame

closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- ⭐ إنشاء حركة النجوم
for i = 1, 50 do
    local star = Instance.new("Frame")
    star.Size = UDim2.new(0,2,0,2)
    star.Position = UDim2.new(math.random(),0,math.random(),0)
    star.BackgroundColor3 = Color3.fromRGB(255,255,255)
    star.BorderSizePixel = 0
    star.AnchorPoint = Vector2.new(0.5,0.5)
    star.Parent = frame

    spawn(function()
        while frame.Parent do
            star.Position = UDim2.new(math.random(),0,math.random(),0)
            star.BackgroundTransparency = math.random()
            wait(math.random(5,15)/10)
        end
    end)
end

-- 🌙 القمر أسفل اليسار
local moon = Instance.new("ImageLabel")
moon.Size = UDim2.new(0, 90, 0, 90)
moon.Position = UDim2.new(0, 10, 1, -100)
moon.BackgroundTransparency = 1
moon.Image = "rbxassetid://6073763716"
moon.Parent = frame
